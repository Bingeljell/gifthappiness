# Copy this file to .env.local and fill in real values once the Worker API
# (see workers/) is deployed. Until then the app falls back to a placeholder
# URL that will not resolve -- that's expected on a machine with no backend
# set up yet.

NEXT_PUBLIC_API_BASE_URL=https://api.gifthappiness.example
