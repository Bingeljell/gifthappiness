# Changelog

- 2026-06-13 > AGENTS.md > repo guidelines > reformatted repo guidance, clarified static-first project direction, documented documentation rules, and captured the guarded `scripts/committer` workflow.
- 2026-06-13 > docs/plan.md > project roadmap > replaced the raw checklist with phased static content, page, charity, design, backend, admin, and engineering cleanup workstreams.
- 2026-06-13 > docs/changelog.md > changelog > created the project changelog and added the first documentation update entry.
- 2026-06-13 > src/app/page.tsx > Home > updated landing page copy, added charity vetting criteria, refreshed featured charity content, and removed placeholder charity actions.
- 2026-06-13 > src/app/create/page.tsx > CreateCelebration > replaced the initial form with the static host, celebration, charity, active date, message, OTP placeholder, and image placeholder fields from the content dump.
- 2026-06-13 > src/app/about/page.tsx > AboutPage > added the Dalai story, charity-first principles, and FAQ content.
- 2026-06-13 > src/app/celebration/page.tsx > CelebrationPage > added a static donor-facing celebration page template with contribution fields and payment placeholder.
- 2026-06-13 > src/components/Header.tsx and src/components/Footer.tsx > navigation > pointed shared links to real routes and removed dead sign-in, privacy, and terms links from the UI.
- 2026-06-13 > src/app/layout.tsx > metadata > updated the site description to match the static celebration page prototype.
- 2026-06-13 > docs/plan.md > project roadmap > marked completed static content, form, page, and placeholder-link tasks.
- 2026-06-13 > docs/plan.md > engineering cleanup > marked the lint cleanup item complete after `npm run lint` passed.
- 2026-06-13 > src/app/layout.tsx and src/app/page.tsx > Home > changed the hero headline font to Playfair Display 600 italic and warmed the hero background toward peach.
- 2026-06-13 > src/app/layout.tsx and src/app/page.tsx > Home > changed the hero headline to Playfair Display 700 normal and removed italic styling.
- 2026-06-13 > src/app/page.tsx > Home > set only the hero word Purpose in primary red while keeping Celebrate with in black.
- 2026-06-13 > src/app/charities/page.tsx > CharitiesPage > added a standalone static charity directory with 12 dummy charities, vetting criteria, and future profile-data placeholders.
- 2026-06-13 > src/components/Header.tsx, src/components/Footer.tsx, and src/app/page.tsx > navigation > routed Browse Charities actions to the new `/charities` page.
- 2026-06-13 > src/app/celebration/page.tsx > CelebrationPage > added a UNICEF context block to make the preview celebration page feel more complete.
- 2026-06-13 > docs/plan.md > charity content > marked the minimum charity profile content task complete and clarified that individual charity detail pages remain undecided.
- 2026-06-14 > src/components/Header.tsx, src/components/Footer.tsx, src/app/create/page.tsx, src/app/page.tsx, src/app/about/page.tsx, src/app/celebration/page.tsx, and src/app/charities/page.tsx > visual styling > split the GiftHappiness wordmark color, changed icon containers to circles, and softened the Create page background and text palette.
- 2026-06-14 > src/app/page.tsx, src/app/celebration/page.tsx, and docs/plan.md > contributor visibility > added cross-celebration contributor messages, a celebration-specific contributor wall, and donor visibility controls with amount privacy by default.
- 2026-06-14 > docs/plan.md > backend planning > selected Cloudflare + Supabase as the provisional backend direction and added CMS, SEO, domain/DNS, payments, and abuse-protection planning sections.
- 2026-08-16 > docs/master_docs.md and docs/website_dev.md > project reference docs > added Master Project Document v1.0 and Website Development Brief v1.0 as the current source of truth for content and IA.
- 2026-08-16 > docs/plan.md > project roadmap > reconciled the plan against the new master_docs.md/website_dev.md, added IA gap tasks (Impact, FAQ nav, Sign In placeholder, footer legal/info pages), new Phase 2 tasks (three-step create flow, QR code, celebration-page copy), new Phase 3 tasks (charity detail pages, SDG tags), and new Phase 3b/3c sections for transparency/impact content and legal/informational page stubs, plus a feasibility note confirming all of it fits the existing Cloudflare static-export setup.
- 2026-08-16 > src/app/privacy/page.tsx, src/app/terms/page.tsx, src/app/refund-policy/page.tsx, src/app/charity-selection-policy/page.tsx, src/app/contact/page.tsx, and src/app/for-charities/page.tsx > legal/info pages > added placeholder pages for the six previously-missing footer links, each marked as pending legal review where applicable.
- 2026-08-16 > src/lib/charities.ts > charity data > added a shared charity dataset (slug, SDG tags, dummy amount raised/ceiling, registration/verification detail) used by the directory, detail pages, and Impact page.
- 2026-08-16 > src/app/charities/page.tsx > CharitiesPage > switched to the shared charity data, added SDG badges and an amount-raised/ceiling line to each card, and linked cards to their detail page.
- 2026-08-16 > src/app/charities/[slug]/page.tsx > CharityDetailPage > added a static per-charity detail route (generateStaticParams) with what/who/why, impact example, SDGs, amount-raised progress bar, and a "Choose this charity" CTA into `/create`.
- 2026-08-16 > src/app/impact/page.tsx > ImpactPage > added a new public Impact page with aggregate totals, an SDG explainer, and a per-charity amount-raised/status table.
- 2026-08-16 > src/app/page.tsx > Home > derived the featured-charity teaser cards from the shared charity data and linked each to its detail page.
- 2026-08-16 > src/components/Header.tsx and src/components/Footer.tsx > navigation > added Impact and FAQ nav links, a non-functional Sign In placeholder, removed the dev-only Preview Page link, and reorganized the footer into Platform/Company/Legal columns covering all documented footer pages.
- 2026-08-16 > package.json, package-lock.json, src/app/create/page.tsx > CreateCelebration > added the `qrcode.react` dependency (user-approved) and rebuilt the create flow as a three-step wizard (Host & Occasion, Cause & Page, Preview & Publish) with a live celebration-page preview and a demo QR code; Publish stays disabled pending a real backend.
- 2026-08-16 > src/app/celebration/page.tsx > CelebrationPage > aligned the donor-facing quote with the suggested copy in `website_dev.md` §8.
- 2026-08-16 > docs/plan.md > project roadmap > marked the Phase 1b/2b/3/3b/3c gap tasks complete now that nav, legal pages, charity detail pages, the Impact page, and the three-step create flow are built.
- 2026-08-16 > supabase/schema.sql > database schema > added the initial Supabase schema (hosts, charities, celebrations, contributions, otp_verifications), RLS enabled with service-role-only access on base tables, and public-safe views (charities_public, celebrations_public, contributions_public) that redact donor name/amount per visibility preference. Not yet applied to a live Supabase project.
- 2026-08-16 > .gitignore > workers subproject > ignored `/workers/node_modules` and `/workers/.wrangler` local dev state.
- 2026-08-16 > workers/package.json, workers/tsconfig.json, workers/wrangler.toml > workers subproject > scaffolded a standalone Cloudflare Worker project (wrangler, @cloudflare/workers-types, @supabase/supabase-js; user-approved) with env bindings documented but no secrets committed. Not deployed.
- 2026-08-16 > workers/src/lib/env.ts, workers/src/lib/supabase.ts, workers/src/lib/response.ts, workers/src/lib/validate.ts > worker helpers > added the Env type, a service-role Supabase client factory, JSON/CORS response helpers, and hand-rolled input validation (no schema library pulled in for this many fields).
- 2026-08-16 > workers/src/routes/celebrations.ts > celebrations API > added POST /celebrations (find-or-create host, insert draft celebration) and GET /celebrations/:slug (reads celebrations_public).
- 2026-08-16 > workers/src/routes/contributions.ts > contributions API > added POST /celebrations/:slug/contributions; records donor intent as payment_status="pending" since no payment gateway is chosen yet.
- 2026-08-16 > workers/src/routes/otp.ts > OTP API > added POST /otp/request and /otp/verify against otp_verifications; codes are hashed and never returned in the response; actual SMS dispatch is a TODO pending provider selection.
- 2026-08-16 > workers/src/routes/admin.ts > admin API > added GET/POST /admin/charities and PATCH /admin/charities/:slug, gated by an ADMIN_API_KEY bearer-token placeholder pending real role-based auth.
- 2026-08-16 > workers/src/index.ts > router > added the fetch handler routing all routes above plus CORS preflight handling and a /health check.
- 2026-08-16 > docs/plan.md > Phase 5 backend > marked schema and Worker route definition complete, and added concrete next steps (create Supabase project, set secrets, deploy, wire frontend, pick OTP provider) now that the code exists locally.
- 2026-08-16 > supabase/schema.sql > hosts/verifications > product decision: host verification switches from mobile OTP to email (OTP deferred, not removed). Added hosts.email/email_verified; renamed otp_verifications to verifications with a channel column ('email'|'mobile') so mobile OTP can be re-enabled later without a schema change.
- 2026-08-16 > workers/src/routes/otp.ts, workers/src/routes/verification.ts > verification API > renamed/generalized requestOtp/verifyOtp to requestVerification/confirmVerification, taking a channel+contact pair instead of mobile-only; email is the active path, dispatch itself stays a TODO pending a provider. Routes moved from /otp/request,/otp/verify to /verify/request,/verify/confirm in workers/src/index.ts.
- 2026-08-16 > workers/src/lib/validate.ts > validation helpers > added requireEmail.
- 2026-08-16 > workers/src/routes/celebrations.ts > celebrations API > createCelebration now requires hostEmail and looks up/creates hosts by email instead of mobile.
- 2026-08-16 > src/lib/api.ts > frontend API client > added a fetch wrapper for the Worker API (requestVerification, confirmVerification, createCelebration, submitContribution), returning {ok,data}|{ok,error} so callers never need try/catch. Reads NEXT_PUBLIC_API_BASE_URL, defaulting to a placeholder that won't resolve until a Worker is deployed.
- 2026-08-16 > .env.example, .gitignore > env plumbing > added NEXT_PUBLIC_API_BASE_URL template; excluded it from the .env* gitignore rule so it stays committable.
- 2026-08-16 > src/app/create/page.tsx > CreateCelebration > replaced the disabled OTP field/button with a live email verification flow (request + confirm), and wired the Publish button to actually call createCelebration, with loading/success/friendly-error states.
- 2026-08-16 > src/app/celebration/page.tsx > CelebrationPage > converted the donor form to controlled inputs, added email and message fields (matching the contributions schema), and wired the submit button to submitContribution against a placeholder "demo" celebration slug (no per-celebration routing exists yet).
- 2026-08-17 > supabase project, workers/wrangler.toml, workers secrets > backend deployment > created a live Supabase project (`fsradcbnqocpvxqwizdt`, ap-south-1, new "GiftHappiness" org) and applied `supabase/schema.sql` (verified all 5 tables + 3 public views exist); set `SUPABASE_URL`/`ALLOWED_ORIGIN` in `workers/wrangler.toml` (`ALLOWED_ORIGIN` is the local dev origin until a production frontend domain exists) and set `SUPABASE_SERVICE_ROLE_KEY`/`ADMIN_API_KEY` as Worker secrets (values never printed to any log; DB password and admin key saved locally only, in gitignored `workers/.supabase-db-password` / `workers/.admin-api-key`).
- 2026-08-17 > workers (deployed) > Cloudflare Worker > deployed to `https://gift-happiness-api.nikhil-shahane.workers.dev`; verified `/health` and a live Supabase-backed route respond correctly.
- 2026-08-17 > .env.local > frontend config > pointed `NEXT_PUBLIC_API_BASE_URL` at the deployed Worker (file is gitignored, not committed).
- 2026-08-17 > supabase (live data), docs/plan.md > end-to-end smoke test > seeded one charity row (`unicef`, copied from `src/lib/charities.ts`, test data only) so `/create` could be exercised fully; ran the full `/create` flow live (email verification request+confirm by reading and brute-forcing the stored code hash for the test contact -- feasible only because it's local test data on a 6-digit code space -- through Publish) and confirmed a real `celebrations` row was created. Also exercised `/celebration`'s donor form live. Found and logged two real gaps in docs/plan.md: celebrations never transition from `draft` to `published` (so nothing published through `/create` is actually readable or contributable yet, despite the UI saying "Published"), and `/celebration` still posts to a hardcoded `"demo"` slug rather than the real one.
- 2026-08-17 > workers/wrangler.toml > CORS config > pointed `ALLOWED_ORIGIN` at the existing Cloudflare Pages Production URL (`https://gifthappiness.pages.dev`, already git-connected to `main`) instead of the local dev origin, and redeployed the Worker. Noted in docs/plan.md that CORS is single-origin only, so Pages Preview deployments and local dev aren't covered by this value at the same time.
- 2026-08-17 > tsconfig.json > TypeScript scope > excluded `workers/` from the root tsconfig's `include` glob. The Cloudflare Pages build was failing (`Cannot find module '@supabase/supabase-js'`) because Next's typecheck was pulling in `workers/**/*.ts`, a separate standalone project with its own `tsconfig.json`/dependencies never meant to be part of the frontend build. Verified with a clean `npm run build` and `npm run lint` locally.
- 2026-08-17 > docs/plan.md > Phase 6 write-up > designed the sign-in/accounts feature (product decision: one unified account for hosts, donors, and admins, not three separate systems) before building it: `hosts` becomes `users` with an `is_admin` flag, `contributions` gains a nullable `donor_id`, a new `sessions` table backs bearer-token auth reusing the existing email-code verification flow, and `/admin/*` moves off the `ADMIN_API_KEY` shared secret onto real sessions. Sequenced as host dashboard first, donor history second, admin RBAC third. No code changed yet.
- 2026-08-17 > supabase/schema.sql (and live project fsradcbnqocpvxqwizdt) > Phase 6 Stage 1 schema > renamed `hosts` to `users` and added `is_admin`; added `contributions.donor_id` (nullable, guest donations still work without it); added `'login'` to `verifications.purpose`; added a new `sessions` table (hashed opaque bearer tokens, not a JWT) with RLS enabled; updated `celebrations_public` to join `users`. Applied by hand against the live DB (no `supabase/migrations` history in this project) and verified the existing test host row survived the rename intact.
- 2026-08-17 > supabase/schema.sql (and live project) > users.name/mobile nullable > relaxed both columns from `not null` since a login-only signup (`POST /auth/request`, not yet built at this point) has neither yet; both are still collected and required by the actual create-celebration/donation forms.
