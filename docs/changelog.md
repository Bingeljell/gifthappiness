# Changelog

- 2026-06-13 > AGENTS.md > repo guidelines > reformatted repo guidance, clarified static-first project direction, documented documentation rules, and captured the guarded `scripts/committer` workflow.
- 2026-06-13 > docs/plan.md > project roadmap > replaced the raw checklist with phased static content, page, charity, design, backend, admin, and engineering cleanup workstreams.
- 2026-06-13 > docs/changelog.md > changelog > created the project changelog and added the first documentation update entry.
- 2026-06-13 > src/app/page.tsx > Home > updated landing page copy, added charity vetting criteria, refreshed featured charity content, and removed placeholder charity actions.
- 2026-06-13 > src/app/create/page.tsx > CreateCelebration > replaced the initial form with the static host, celebration, charity, active date, message, OTP placeholder, and image placeholder fields from the content dump.
- 2026-06-13 > src/app/about/page.tsx > AboutPage > added the Dalai story, charity-first principles, and FAQ content.
- 2026-06-13 > src/app/celebration/page.tsx > CelebrationPage > added a static donor-facing celebration page template with contribution fields and payment placeholder.
- 2026-06-13 > src/components/Header.tsx and src/components/Footer.tsx > navigation > pointed shared links to real routes and removed dead sign-in, privacy, and terms links from the UI.
- 2026-06-13 > src/app/layout.tsx > metadata > updated the site description to match the static celebration page prototype.
- 2026-06-13 > docs/plan.md > project roadmap > marked completed static content, form, page, and placeholder-link tasks.
- 2026-06-13 > docs/plan.md > engineering cleanup > marked the lint cleanup item complete after `npm run lint` passed.
- 2026-06-13 > src/app/layout.tsx and src/app/page.tsx > Home > changed the hero headline font to Playfair Display 600 italic and warmed the hero background toward peach.
- 2026-06-13 > src/app/layout.tsx and src/app/page.tsx > Home > changed the hero headline to Playfair Display 700 normal and removed italic styling.
- 2026-06-13 > src/app/page.tsx > Home > set only the hero word Purpose in primary red while keeping Celebrate with in black.
- 2026-06-13 > src/app/charities/page.tsx > CharitiesPage > added a standalone static charity directory with 12 dummy charities, vetting criteria, and future profile-data placeholders.
- 2026-06-13 > src/components/Header.tsx, src/components/Footer.tsx, and src/app/page.tsx > navigation > routed Browse Charities actions to the new `/charities` page.
- 2026-06-13 > src/app/celebration/page.tsx > CelebrationPage > added a UNICEF context block to make the preview celebration page feel more complete.
- 2026-06-13 > docs/plan.md > charity content > marked the minimum charity profile content task complete and clarified that individual charity detail pages remain undecided.
- 2026-06-14 > src/components/Header.tsx, src/components/Footer.tsx, src/app/create/page.tsx, src/app/page.tsx, src/app/about/page.tsx, src/app/celebration/page.tsx, and src/app/charities/page.tsx > visual styling > split the GiftHappiness wordmark color, changed icon containers to circles, and softened the Create page background and text palette.
- 2026-06-14 > src/app/page.tsx, src/app/celebration/page.tsx, and docs/plan.md > contributor visibility > added cross-celebration contributor messages, a celebration-specific contributor wall, and donor visibility controls with amount privacy by default.
- 2026-06-14 > docs/plan.md > backend planning > selected Cloudflare + Supabase as the provisional backend direction and added CMS, SEO, domain/DNS, payments, and abuse-protection planning sections.
- 2026-08-16 > docs/master_docs.md and docs/website_dev.md > project reference docs > added Master Project Document v1.0 and Website Development Brief v1.0 as the current source of truth for content and IA.
- 2026-08-16 > docs/plan.md > project roadmap > reconciled the plan against the new master_docs.md/website_dev.md, added IA gap tasks (Impact, FAQ nav, Sign In placeholder, footer legal/info pages), new Phase 2 tasks (three-step create flow, QR code, celebration-page copy), new Phase 3 tasks (charity detail pages, SDG tags), and new Phase 3b/3c sections for transparency/impact content and legal/informational page stubs, plus a feasibility note confirming all of it fits the existing Cloudflare static-export setup.
- 2026-08-16 > src/app/privacy/page.tsx, src/app/terms/page.tsx, src/app/refund-policy/page.tsx, src/app/charity-selection-policy/page.tsx, src/app/contact/page.tsx, and src/app/for-charities/page.tsx > legal/info pages > added placeholder pages for the six previously-missing footer links, each marked as pending legal review where applicable.
- 2026-08-16 > src/lib/charities.ts > charity data > added a shared charity dataset (slug, SDG tags, dummy amount raised/ceiling, registration/verification detail) used by the directory, detail pages, and Impact page.
- 2026-08-16 > src/app/charities/page.tsx > CharitiesPage > switched to the shared charity data, added SDG badges and an amount-raised/ceiling line to each card, and linked cards to their detail page.
- 2026-08-16 > src/app/charities/[slug]/page.tsx > CharityDetailPage > added a static per-charity detail route (generateStaticParams) with what/who/why, impact example, SDGs, amount-raised progress bar, and a "Choose this charity" CTA into `/create`.
- 2026-08-16 > src/app/impact/page.tsx > ImpactPage > added a new public Impact page with aggregate totals, an SDG explainer, and a per-charity amount-raised/status table.
- 2026-08-16 > src/app/page.tsx > Home > derived the featured-charity teaser cards from the shared charity data and linked each to its detail page.
- 2026-08-16 > src/components/Header.tsx and src/components/Footer.tsx > navigation > added Impact and FAQ nav links, a non-functional Sign In placeholder, removed the dev-only Preview Page link, and reorganized the footer into Platform/Company/Legal columns covering all documented footer pages.
- 2026-08-16 > package.json, package-lock.json, src/app/create/page.tsx > CreateCelebration > added the `qrcode.react` dependency (user-approved) and rebuilt the create flow as a three-step wizard (Host & Occasion, Cause & Page, Preview & Publish) with a live celebration-page preview and a demo QR code; Publish stays disabled pending a real backend.
- 2026-08-16 > src/app/celebration/page.tsx > CelebrationPage > aligned the donor-facing quote with the suggested copy in `website_dev.md` §8.
- 2026-08-16 > docs/plan.md > project roadmap > marked the Phase 1b/2b/3/3b/3c gap tasks complete now that nav, legal pages, charity detail pages, the Impact page, and the three-step create flow are built.
- 2026-08-16 > supabase/schema.sql > database schema > added the initial Supabase schema (hosts, charities, celebrations, contributions, otp_verifications), RLS enabled with service-role-only access on base tables, and public-safe views (charities_public, celebrations_public, contributions_public) that redact donor name/amount per visibility preference. Not yet applied to a live Supabase project.
- 2026-08-16 > .gitignore > workers subproject > ignored `/workers/node_modules` and `/workers/.wrangler` local dev state.
- 2026-08-16 > workers/package.json, workers/tsconfig.json, workers/wrangler.toml > workers subproject > scaffolded a standalone Cloudflare Worker project (wrangler, @cloudflare/workers-types, @supabase/supabase-js; user-approved) with env bindings documented but no secrets committed. Not deployed.
- 2026-08-16 > workers/src/lib/env.ts, workers/src/lib/supabase.ts, workers/src/lib/response.ts, workers/src/lib/validate.ts > worker helpers > added the Env type, a service-role Supabase client factory, JSON/CORS response helpers, and hand-rolled input validation (no schema library pulled in for this many fields).
- 2026-08-16 > workers/src/routes/celebrations.ts > celebrations API > added POST /celebrations (find-or-create host, insert draft celebration) and GET /celebrations/:slug (reads celebrations_public).
- 2026-08-16 > workers/src/routes/contributions.ts > contributions API > added POST /celebrations/:slug/contributions; records donor intent as payment_status="pending" since no payment gateway is chosen yet.
- 2026-08-16 > workers/src/routes/otp.ts > OTP API > added POST /otp/request and /otp/verify against otp_verifications; codes are hashed and never returned in the response; actual SMS dispatch is a TODO pending provider selection.
- 2026-08-16 > workers/src/routes/admin.ts > admin API > added GET/POST /admin/charities and PATCH /admin/charities/:slug, gated by an ADMIN_API_KEY bearer-token placeholder pending real role-based auth.
- 2026-08-16 > workers/src/index.ts > router > added the fetch handler routing all routes above plus CORS preflight handling and a /health check.
- 2026-08-16 > docs/plan.md > Phase 5 backend > marked schema and Worker route definition complete, and added concrete next steps (create Supabase project, set secrets, deploy, wire frontend, pick OTP provider) now that the code exists locally.
- 2026-08-16 > supabase/schema.sql > hosts/verifications > product decision: host verification switches from mobile OTP to email (OTP deferred, not removed). Added hosts.email/email_verified; renamed otp_verifications to verifications with a channel column ('email'|'mobile') so mobile OTP can be re-enabled later without a schema change.
- 2026-08-16 > workers/src/routes/otp.ts, workers/src/routes/verification.ts > verification API > renamed/generalized requestOtp/verifyOtp to requestVerification/confirmVerification, taking a channel+contact pair instead of mobile-only; email is the active path, dispatch itself stays a TODO pending a provider. Routes moved from /otp/request,/otp/verify to /verify/request,/verify/confirm in workers/src/index.ts.
- 2026-08-16 > workers/src/lib/validate.ts > validation helpers > added requireEmail.
- 2026-08-16 > workers/src/routes/celebrations.ts > celebrations API > createCelebration now requires hostEmail and looks up/creates hosts by email instead of mobile.
- 2026-08-16 > src/lib/api.ts > frontend API client > added a fetch wrapper for the Worker API (requestVerification, confirmVerification, createCelebration, submitContribution), returning {ok,data}|{ok,error} so callers never need try/catch. Reads NEXT_PUBLIC_API_BASE_URL, defaulting to a placeholder that won't resolve until a Worker is deployed.
- 2026-08-16 > .env.example, .gitignore > env plumbing > added NEXT_PUBLIC_API_BASE_URL template; excluded it from the .env* gitignore rule so it stays committable.
- 2026-08-16 > src/app/create/page.tsx > CreateCelebration > replaced the disabled OTP field/button with a live email verification flow (request + confirm), and wired the Publish button to actually call createCelebration, with loading/success/friendly-error states.
- 2026-08-16 > src/app/celebration/page.tsx > CelebrationPage > converted the donor form to controlled inputs, added email and message fields (matching the contributions schema), and wired the submit button to submitContribution against a placeholder "demo" celebration slug (no per-celebration routing exists yet).
- 2026-08-17 > supabase project, workers/wrangler.toml, workers secrets > backend deployment > created a live Supabase project (`fsradcbnqocpvxqwizdt`, ap-south-1, new "GiftHappiness" org) and applied `supabase/schema.sql` (verified all 5 tables + 3 public views exist); set `SUPABASE_URL`/`ALLOWED_ORIGIN` in `workers/wrangler.toml` (`ALLOWED_ORIGIN` is the local dev origin until a production frontend domain exists) and set `SUPABASE_SERVICE_ROLE_KEY`/`ADMIN_API_KEY` as Worker secrets (values never printed to any log; DB password and admin key saved locally only, in gitignored `workers/.supabase-db-password` / `workers/.admin-api-key`).
- 2026-08-17 > workers (deployed) > Cloudflare Worker > deployed to `https://gift-happiness-api.nikhil-shahane.workers.dev`; verified `/health` and a live Supabase-backed route respond correctly.
- 2026-08-17 > .env.local > frontend config > pointed `NEXT_PUBLIC_API_BASE_URL` at the deployed Worker (file is gitignored, not committed).
- 2026-08-17 > supabase (live data), docs/plan.md > end-to-end smoke test > seeded one charity row (`unicef`, copied from `src/lib/charities.ts`, test data only) so `/create` could be exercised fully; ran the full `/create` flow live (email verification request+confirm by reading and brute-forcing the stored code hash for the test contact -- feasible only because it's local test data on a 6-digit code space -- through Publish) and confirmed a real `celebrations` row was created. Also exercised `/celebration`'s donor form live. Found and logged two real gaps in docs/plan.md: celebrations never transition from `draft` to `published` (so nothing published through `/create` is actually readable or contributable yet, despite the UI saying "Published"), and `/celebration` still posts to a hardcoded `"demo"` slug rather than the real one.
- 2026-08-17 > workers/wrangler.toml > CORS config > pointed `ALLOWED_ORIGIN` at the existing Cloudflare Pages Production URL (`https://gifthappiness.pages.dev`, already git-connected to `main`) instead of the local dev origin, and redeployed the Worker. Noted in docs/plan.md that CORS is single-origin only, so Pages Preview deployments and local dev aren't covered by this value at the same time.
- 2026-08-17 > tsconfig.json > TypeScript scope > excluded `workers/` from the root tsconfig's `include` glob. The Cloudflare Pages build was failing (`Cannot find module '@supabase/supabase-js'`) because Next's typecheck was pulling in `workers/**/*.ts`, a separate standalone project with its own `tsconfig.json`/dependencies never meant to be part of the frontend build. Verified with a clean `npm run build` and `npm run lint` locally.
- 2026-08-17 > docs/plan.md > Phase 6 write-up > designed the sign-in/accounts feature (product decision: one unified account for hosts, donors, and admins, not three separate systems) before building it: `hosts` becomes `users` with an `is_admin` flag, `contributions` gains a nullable `donor_id`, a new `sessions` table backs bearer-token auth reusing the existing email-code verification flow, and `/admin/*` moves off the `ADMIN_API_KEY` shared secret onto real sessions. Sequenced as host dashboard first, donor history second, admin RBAC third. No code changed yet.
- 2026-08-17 > supabase/schema.sql (and live project fsradcbnqocpvxqwizdt) > Phase 6 Stage 1 schema > renamed `hosts` to `users` and added `is_admin`; added `contributions.donor_id` (nullable, guest donations still work without it); added `'login'` to `verifications.purpose`; added a new `sessions` table (hashed opaque bearer tokens, not a JWT) with RLS enabled; updated `celebrations_public` to join `users`. Applied by hand against the live DB (no `supabase/migrations` history in this project) and verified the existing test host row survived the rename intact.
- 2026-08-17 > supabase/schema.sql (and live project) > users.name/mobile nullable > relaxed both columns from `not null` since a login-only signup (`POST /auth/request`, not yet built at this point) has neither yet; both are still collected and required by the actual create-celebration/donation forms.
- 2026-08-17 > workers/src/routes/verification.ts, workers/src/routes/celebrations.ts, workers/src/lib/code.ts > hosts → users rename > extracted the shared 6-digit code generate/hash helpers into `lib/code.ts` (was duplicated verbatim, now also needed by the upcoming login route) and updated all `.from("hosts")` references to `.from("users")` after the Phase 6 rename. `createCelebration` now also backfills name/mobile/address onto a pre-existing user record (e.g. one created via login-only signup) instead of assuming a host row is always brand new. `npm run typecheck` in `workers/` passes.
- 2026-08-17 > workers/src/lib/session.ts, workers/src/routes/auth.ts, workers/src/routes/me.ts, workers/src/index.ts > Phase 6 Stage 1 auth backend > added `POST /auth/request` (find-or-create user by email + issue a login code, purpose `"login"` in the same `verifications` table), `POST /auth/confirm` (validate code, mark `email_verified`, issue a session), `GET /auth/me`, `POST /auth/logout`, and `GET /me/celebrations` (session-gated, unlike the public `GET /celebrations/:slug` which only shows published ones). Sessions are opaque bearer tokens hashed at rest in the new `sessions` table, not a JWT (see docs/plan.md Phase 6 for why). `npm run typecheck` in `workers/` passes; not deployed yet in this commit.
- 2026-08-17 > workers (deployed), curl smoke test > Phase 6 Stage 1 backend verified live > deployed the auth routes and ran the full flow against the live Worker+Supabase: `/auth/request` → looked up the issued code's hash directly in Supabase and brute-forced the matching 6-digit code (own test data, feasible only because of the small code space, same technique used earlier for host-signup verification) → `/auth/confirm` returned a real session token → `/auth/me` and `/me/celebrations` both resolved correctly with it → `/auth/logout` deleted the session, confirmed `/auth/me` then correctly 401s. Backend is solid end to end.
- 2026-08-17 > src/lib/api.ts, src/lib/session.tsx, src/app/layout.tsx, src/app/sign-in/page.tsx, src/app/account/page.tsx, src/components/Header.tsx > Phase 6 Stage 1 frontend > added `requestLogin`/`confirmLogin`/`getMe`/`logout`/`getMyCelebrations` to the API client; added `SessionProvider`/`useSession` (bearer token in `localStorage`, not a cookie, per the Phase 6 write-up's cross-origin reasoning) wired into the root layout; built `/sign-in` (email → code → confirm) and `/account` (client-side protected, lists the signed-in user's celebrations); replaced `Header.tsx`'s greyed-out "Sign In" placeholder with a real link, plus a signed-in state (name/email link to `/account`, Sign Out). `npm run build` and `npm run lint` both pass. Verified live in the browser through the request-code step (network request confirmed 201 from the deployed Worker, code UI transition works); the confirm-code → `/account` redirect step is coded and typechecked against the same backend endpoints already curl-verified above, but wasn't clicked through in the browser before this session paused -- next step on resume is finishing that click-through.
- 2026-08-17 > workers/wrangler.toml > temporary CORS flip, reverted > briefly set `ALLOWED_ORIGIN` to `http://localhost:3000` to browser-test the sign-in flow against the live Worker (Worker CORS is single-origin, see the Phase 5 entry above), then reverted to `https://gifthappiness.pages.dev` and redeployed before pausing. Confirmed via `git status`/`grep` that the live Worker and the committed file both ended back on the production origin -- production was not left broken.
- 2026-08-17 > (browser verification, no code changes) > Phase 6 Stage 1 fully verified > resumed and finished the browser click-through: repeated the temporary `ALLOWED_ORIGIN` flip to test live, then confirmed the full sign-in → `/account` → sign-out flow works correctly end to end (see docs/plan.md Phase 6 Stage 1 for the detailed steps), reverted `ALLOWED_ORIGIN` back to the production Pages origin, and verified via a live CORS check (with a retry after a few seconds of Cloudflare edge propagation lag) that production is serving the correct origin again. Phase 6 Stage 1 is complete with nothing outstanding.
- 2026-08-19 > supabase/schema.sql, workers/src/lib/session.ts, workers/src/routes/contributions.ts, workers/src/routes/me.ts, workers/src/index.ts > Phase 6 Stage 2 backend > added `contributions_donor_id_idx` (not yet applied to the live project); exposed `mobile` on `SessionUser`/`getSessionUser`; `submitContribution` now attaches `donor_id` from the session best-effort, without gating the route (guest donations keep working); added `GET /me/contributions` (session-gated, reads the base `contributions` table, resolves each contribution's celebration slug/type via a small follow-up query since there's no join precedent elsewhere in `routes/*.ts`). `npm run typecheck` in `workers/` passes; not deployed yet.
- 2026-08-19 > src/lib/api.ts, src/app/celebration/page.tsx, src/app/account/page.tsx > Phase 6 Stage 2 frontend > added `mobile` to the `User` type, `MyContribution` type, `getMyContributions`, and an optional session-token param on `submitContribution`; `/celebration`'s donor name/email/mobile fields now pre-fill from the signed-in user's account (still editable, never clobbering an in-progress edit — implemented via React's render-time state-adjustment pattern rather than an effect, to satisfy this repo's `set-state-in-effect` lint rule) and the form now sends the session token when submitting; `/account` gained a "Your past donations" card matching the existing celebrations card's style/states. `npm run lint` and `npm run build` both pass.
- 2026-08-19 > workers/src/routes/admin.ts > Phase 6 Stage 3 admin RBAC > `isAuthorizedAdmin` is now async and checks the session's `isAdmin` flag, falling back to the existing `ADMIN_API_KEY` shared-secret bearer match (kept intentionally, not deleted, per docs/plan.md's transition note). Updated all 3 call sites (`listCharitiesAdmin`, `createCharity`, `updateCharity`) to `await` it. `npm run typecheck` in `workers/` passes.
- 2026-08-19 > src/lib/api.ts, src/app/admin/page.tsx, src/components/Header.tsx > Phase 6 Stage 3 admin frontend > added `AdminCharity` type and `adminListCharities`; added a new read-only `/admin` page (three states: redirect to `/sign-in` if signed out, inline "no access" message if signed in but not an admin, charities list if admin — reusing the account page's card shell/state-machine style); `Header.tsx` now shows an "Admin" nav link (desktop + mobile) only when `user.isAdmin`. No create/edit charity UI in this pass — that stays API-only. `npm run lint` and `npm run build` both pass.
- 2026-08-19 > workers/src/lib/email.ts, workers/src/lib/env.ts, workers/wrangler.toml, workers/src/routes/auth.ts, workers/src/routes/verification.ts > real email delivery > wired login/verification codes to actual email delivery via Resend's HTTP API (plain `fetch`, no SDK dependency), added `RESEND_API_KEY` as a Worker secret. Sends from Resend's shared sandbox address until `gifthappiness.com` is verified as a sending domain there; until then, deliverable only to the email on the Resend account itself. Deployed and verified live (a real sign-in code arrived by email).
- 2026-08-19 > src/app/sign-in/page.tsx, src/app/create/page.tsx > UI copy cleanup > removed the "email delivery isn't wired up yet" hint text, shown unconditionally on the code-entry step regardless of actual delivery status — stale now that Resend is wired up.
- 2026-08-19 > docs/plan.md > Phase 7 write-up (documentation only, no code) > found that every public-facing charity page (`/charities`, `/charities/[slug]`, `/`, `/create`, `/impact`) still reads a static hardcoded 12-charity file (`src/lib/charities.ts`) rather than the live `charities` table — meaning charities created via the already-built `/admin` write routes are invisible on the actual site. Documented the gap and a 4-step plan (public `GET /charities` endpoint reading the existing unused `charities_public` view, rewiring the 5 static-import pages to it, an admin create-charity form with an auto-generated slug, then deleting the dummy file) after confirming the real admin will be non-technical (a friend and his daughter, onboarding others herself) and can't use curl/SQL. Resolved two long-open decision points in "CMS And Admin Direction" and "Admin-Controlled Content" accordingly.
- 2026-08-19 > workers/src/routes/charities.ts, workers/src/index.ts > Phase 7 backend > added public `GET /charities` and `GET /charities/:slug`, reading the existing `charities_public` view as-is (already granted `select` to `anon`, no extra filtering needed). `npm run typecheck` in `workers/` passes; not deployed yet.
- 2026-08-19 > src/lib/api.ts, src/lib/sdgs.ts, src/app/charities/page.tsx, src/app/charities/[slug]/page.tsx, src/app/charities/[slug]/CharityDetailClient.tsx, public/_redirects > Phase 7 charity pages > added the `Charity` type and `getCharities`/`getCharity` to the API client; rewired the charity directory to fetch live data; rewired the detail page to a Client-Component "shell" pattern since static export can't pre-render one file per dynamic charity slug anymore (`generateStaticParams` returns a single `_shell` param; `public/_redirects` rewrites any `/charities/*` request to it with a 200 so the browser URL keeps the real slug; the shell reads the slug from `window.location` and fetches it) — see docs/plan.md Phase 7 for the full rationale. Extracted `sdgDescriptions` out of the now-static-data-only file into its own `src/lib/sdgs.ts` (charity-independent reference data). Also fixed the stale "No backend is deployed yet" comment in `src/lib/api.ts`.
- 2026-08-19 > src/app/page.tsx, src/app/create/page.tsx, src/app/impact/page.tsx > Phase 7 remaining pages > rewired the homepage's featured-charity teasers (now the first 3 live charities, badge derived from category initials instead of a hardcoded per-slug label map), `/create`'s charity-selection step (default selection set via React's render-time state-adjustment pattern, publish blocked with a clear error if no charity is selected), and `/impact`'s totals/table to fetch from the live endpoint instead of the static file. Also removed stale "dummy data"/"backend isn't deployed" copy from `/create` and fixed the `status === "Active"` comparison to match the DB's lowercase `active`/`completed` values.
- 2026-08-19 > src/lib/api.ts, src/app/admin/AddCharityForm.tsx, src/app/admin/page.tsx > Phase 7 admin create-charity form > added `AdminCreateCharityInput`/`adminCreateCharity` to the API client and a collapsible create-charity form on `/admin` (auto-generates the slug from the charity name -- shown as a read-only preview, never asked of the admin -- and includes an SDG checkbox list sourced from `src/lib/sdgs.ts`). New charities prepend to the existing list on success without a refetch.
- 2026-08-19 > src/lib/charities.ts (deleted) > Phase 7 cleanup > removed the static 12-charity dummy dataset now that every page reads live data; confirmed via grep that nothing still imported it before deleting.
- 2026-08-19 > (deployed, no code changes) > Phase 7 verified live > merged #12, deployed the Worker, added a test charity via `/admin`, and confirmed the `_shell`/`_redirects` rewrite works correctly on real Cloudflare Pages.
- 2026-08-19 > supabase/schema.sql > Phase 7 follow-up: schema > dropped `charities.ceiling` (not yet applied live -- manual migration needed, see docs/plan.md), added `charities.website`. Product decision: GiftHappiness has no per-charity fundraising target/cap, so there's nothing to frame `amount_raised` against.
- 2026-08-19 > workers/src/routes/admin.ts > Phase 7 follow-up: backend > removed `ceiling` from `createCharity`/`updateCharity` (and its positive-number validation), added `website` to both. `npm run typecheck` in `workers/` passes.
- 2026-08-19 > src/lib/api.ts > Phase 7 follow-up: API client > removed `ceiling` from `Charity`/`AdminCharity`/`AdminCreateCharityInput`, added `website: string | null` / `website?: string`.
- 2026-08-19 > src/app/admin/AddCharityForm.tsx > Phase 7 follow-up: admin form > removed the fundraising-goal field and its validation; added an optional Website field; added a hint + placeholder on the "What they do"/"Who they help"/"Why selected"/"Impact example" textareas ("one point per line") and bumped their row count, since those are now rendered as bulleted lists, not paragraphs.
- 2026-08-19 > src/app/charities/[slug]/CharityDetailClient.tsx > Phase 7 follow-up: charity detail page redesign > removed the amount-raised progress bar and "of ₹X" text; added a "Visit their website" link when `charity.website` is set; `InfoBlock` gained a `list` mode that splits on newlines into an actual `<ul>` (stripping leading `-`/`1.`/`•` markers), used for what-they-do/who-they-help/why-selected/impact-example; restacked those from a cramped 3-column grid to full-width cards (direct fix for the wall-of-text look reported after testing with real content).
- 2026-08-19 > src/app/admin/page.tsx, src/app/charities/page.tsx, src/app/impact/page.tsx > Phase 7 follow-up: remaining ceiling references > removed the "of ₹X ceiling" text from the admin charities list and the charity directory cards, and the Ceiling column from the Impact table.
- 2026-08-19 > src/app/page.tsx, src/app/about/page.tsx, src/app/charity-selection-policy/page.tsx, src/app/charities/page.tsx > Phase 7 follow-up: policy copy > removed "charities rotate out after reaching a predetermined [donation/contribution] limit" language from the homepage, the charity directory's vetting criteria, the About FAQ, and the Charity Selection Policy page -- that policy no longer exists once there's no fundraising ceiling to reach.
- 2026-08-19 > (live SQL, no code) > Phase 7 charity-content migration applied > ran the corrected migration (drop `charities_public` before altering `ceiling`, not `create or replace`, which fails when removing a column from an existing view's output) against the live Supabase project. Confirmed working.
- 2026-08-19 > docs/plan.md > migration doc fix > corrected the Phase 7 migration snippet to match what actually worked live (drop view -> alter table -> recreate view -> re-grant select), after the original `create or replace view` ordering failed live with a real dependency error.
- 2026-08-19 > workers/src/lib/response.ts > CORS bug fix > `Access-Control-Allow-Methods` only listed `GET, POST, OPTIONS` -- `PATCH` (used by `updateCharity`, live since Phase 6 Stage 3 but never called from the browser until now) would have failed at the CORS preflight stage regardless of frontend code. Added `PATCH`.
- 2026-08-19 > src/app/admin/CharityFormFields.tsx, src/app/admin/AddCharityForm.tsx > shared form fields > extracted `FormField`/`SdgChecklist` out of `AddCharityForm.tsx` into their own file so the new edit form doesn't duplicate ~80 lines of field-rendering markup.
- 2026-08-19 > src/lib/api.ts > admin update API client > added `AdminUpdateCharityInput`/`adminUpdateCharity` (`PATCH /admin/charities/:slug`); uses snake_case keys directly, matching how the backend route reads its `allowedFields` off the raw request body (unlike the camelCase-mapped create route).
- 2026-08-19 > src/app/admin/EditCharityForm.tsx, src/app/admin/page.tsx > charity edit UI > added an edit form (slug read-only, `status` and `amount_raised` newly editable alongside every create-form field) and wired an Edit button into each `/admin` charity row that swaps it in place; saving updates that one charity in local state without a refetch. `npm run typecheck` (workers) and `npm run lint`/`npm run build` (frontend) all pass. Not yet deployed -- needs a Worker redeploy for the CORS fix to take effect.
- 2026-09-01 > src/app/page.tsx > remove fabricated testimonials > deleted the "Real Stories" hero quote (fake "Sarah M.") and the "Contributor Messages" section (fake names, two referencing charities -- WWF, Save the Children -- not present in the live charity list); no real contribution/testimonial data exists yet to replace them with.
- 2026-09-01 > supabase/schema.sql, workers/src/routes/uploads.ts, workers/src/routes/admin.ts, workers/src/index.ts, src/lib/api.ts, src/app/admin/CharityFormFields.tsx, src/app/admin/AddCharityForm.tsx, src/app/admin/EditCharityForm.tsx, src/components/CharityBadge.tsx, src/app/page.tsx, src/app/charities/page.tsx, src/app/charities/[slug]/CharityDetailClient.tsx > Phase 8 charity pictures > added `charities.logo_url` + a public Supabase Storage bucket (`charity-logos`); new admin-gated `POST /admin/uploads/charity-logo` upload endpoint; wired an image upload field into the admin create/edit charity forms and a `CharityBadge` component (logo or category-initial fallback) into every public charity card/detail view. `npm run lint`/`npm run build` (frontend) and `npx tsc --noEmit` (workers) all pass.
- 2026-09-01 > (live SQL + wrangler deploy, no further code changes) > Phase 8 applied live > ran the `logo_url` column/storage-bucket/view migration against the live Supabase project via `supabase db query --linked` and deployed the Worker (`wrangler deploy`), which also shipped the previously-pending Phase 7 CORS `PATCH` fix. Verified end to end: `GET /charities/unicef` returns `logo_url`; a real authenticated upload returned a working public image URL; unauthenticated upload correctly 401s. See docs/plan.md Phase 8 for details.
